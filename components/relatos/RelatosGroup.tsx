import { Relato } from "escolas-shared";
import React from 'react';

type RelatosGroupProps = {
  relatos: Relato[];
};

const RelatosGroup: React.FC<RelatosGroupProps> = (props) => {
  return (
    <div>(TODO...)</div>
  );
};

export default React.memo(RelatosGroup);
